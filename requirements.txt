# --- Core backend (required) -------------------------------------------------
fastapi==0.136.3
uvicorn==0.49.0
requests==2.34.2
bcrypt==5.0.0
PyJWT==2.13.0
pydantic==2.13.4
python-dotenv==1.0.1
cachetools==5.5.2
python-multipart==0.0.20

# --- Production server (Task 4 — Linux PaaS: Render / Railway) ----------------
# gunicorn manages uvicorn workers on a live host. Not used for local dev on
# Windows (uvicorn's own runner is used there); harmless to install regardless.
gunicorn==23.0.0

# --- OCR Label Scanner POC (Task 6) ------------------------------------------
# Python side of the OCR stack. The Tesseract *engine* is a separate native
# install (see DEPLOYMENT.md / ocr_label_scanner.py). If these are absent the app
# still runs — the /ocr/* endpoints just report that OCR is unavailable.
pytesseract==0.3.13
Pillow==11.3.0
